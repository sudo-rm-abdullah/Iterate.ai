# ProjectPulse

Chrome Extension (Manifest V3) that automatically tracks ML/data-science project progress across Google Colab and GitHub.

See [`extension/README.md`](extension/README.md) for setup and load instructions.

## Quick start

```bash
cd extension
npm install
npm run build
```

Load `extension/dist` as an unpacked extension in `chrome://extensions`.
